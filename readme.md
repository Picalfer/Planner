## Autostart

1. Before creating the shortcut, replace all paths in `start_django.bat` and `start.vbs` with your actual project path
2. Create a shortcut for `start.vbs`
3. Press `Win + R`, type `shell:startup`, press Enter
4. Move the shortcut to this folder